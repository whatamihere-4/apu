# Edit this file for your domain, TLS, auth, or other Caddy features.
# The upstream app container is always named "apu" on the Docker network.
#
# Set APU_DOMAIN in .env (defaults to localhost for local use).
# For a public hostname with automatic HTTPS, set e.g. APU_DOMAIN=apu.example.com
# and ACME_EMAIL=you@example.com in .env.

{
	email {$ACME_EMAIL}
}

{$APU_DOMAIN:localhost} {
	reverse_proxy apu:5000
}
